# คู่มือการใช้งาน — AD Dat File

ระบบดึงข้อมูล log จากเครื่อง AD เข้าฐานข้อมูล และคัดลอกไฟล์ไปยังปลายทางต่าง ๆ

| | |
|---|---|
| **ที่อยู่ไฟล์จริง** | `D:\BIINA\` (เครื่องที่รันสคริปต์) |
| **ต้นฉบับ** | `Z:\DI\Nadia\AD Dat file` |
| **Python** | `D:\WPy64-31150\python-3.11.5.amd64\python.exe` |
| **ฐานข้อมูล** | `172.18.106.100` |
| **อัปเดตล่าสุด** | 2026-08-08 |

---

## 1. ภาพรวม — มี 5 สคริปต์ที่ใช้งานจริง

ทุกสคริปต์อ่านรายชื่อเครื่องและ IP จากตาราง `vipdatabase.ad_ip` (ยกเว้น `recipe.py` ที่กำหนดรายชื่อเครื่องเอง)

| ไฟล์ .bat ที่กด | เรียกสคริปต์ | ทำอะไร | ปลายทาง |
|---|---|---|---|
| `AD Log Data.bat` | `AD Log DataV4.py` | ดึง log ย้อนหลัง **60 วัน** เข้า DB (รันอัตโนมัติ ไม่มี pause) | `inadatabase.ad_log3` |
| `AD Log Data - Copy.bat` | `AD Log DataV3_new.py` | ดึง log ย้อนหลัง **180 วัน** เข้า DB (รันมือ มี pause) | `inadatabase.ad_log3` |
| `ADCopyDatfiles.bat` | `AD Copy Dat filesv4.py` | คัดลอกไฟล์ `.dat` | `\\172.18.106.224\mc_csv6\QC\logad\{เครื่อง}` |
| `AD auto.bat` | `AD auto.py` | คัดลอกไฟล์ `.log` จากโฟลเดอร์ Auto | `\\172.18.106.55\FileTransfer_IT-OT\DI\addata\auto\{เครื่อง}` |
| `ADrecipe.bat` | `recipe.py` | **ทิศทางกลับกัน** — ดึง recipe จาก DB ไปเขียนทับไฟล์ setting ที่ตัวเครื่อง | `\\{IP}\{Version}\Master list{เครื่อง}.txt` |

> ⚠️ `AD Log DataV4.py` กับ `AD Log DataV3_new.py` **เขียนลงตารางเดียวกัน** (`ad_log3`)
> และเขียน log ไฟล์เดียวกัน (`ad_log2_run.log`) — ไม่ควรรันพร้อมกัน

### Factory A หรือ E ตัดสินจากชื่อเครื่อง PC

```python
factory = "A" if socket.gethostname() == "2592P-ED363" else "E"
```

ถ้ารันจากเครื่องอื่นที่ไม่ใช่ `2592P-ED363` ระบบจะถือว่าเป็น **Factory E เสมอ** — ต้องรันจากเครื่องที่ถูกต้องเท่านั้น

---

## 2. วิธีรัน

ดับเบิลคลิกไฟล์ `.bat` ที่ต้องการ หรือเรียกจาก Command Prompt

**แนะนำให้เริ่มจาก `AD Log Data - Copy.bat`** เพราะมี `pause` ค้างหน้าจอไว้ให้อ่านผลลัพธ์ได้ก่อนปิด

หลังรันเสร็จ ให้เปิดไฟล์ `ad_log2_run.log` ตรวจบรรทัดสุดท้าย ควรเห็น

```
Insert สำเร็จ 1234/1234 แถว → ad_log3
```

**ตัวเลขสองตัวต้องเท่ากัน** ถ้าไม่เท่ากันแปลว่ามีแถวที่เขียนไม่สำเร็จ ดูหัวข้อ 5

---

## 3. การตัดเครื่องออกจากระบบ (EXCLUDE_MACHINES)

ทุกสคริปต์มีตัวแปรนี้อยู่ด้านบนของไฟล์

```python
EXCLUDE_MACHINES = {"AD05"}
```

เครื่องที่อยู่ในชุดนี้จะถูกข้ามทุกรอบ

### ปัจจุบันตัด AD05 ออกแล้ว (2026-08-08)

เหตุผล: จาก log 12 รอบที่รันจริง (7 เม.ย.–23 ก.ค. 2026) **AD05 เชื่อมต่อ path ไม่ได้ 22 ครั้ง** ซึ่งสูงที่สุด
ข้อมูลของ AD05 แทบไม่เคยเข้า `ad_log3` อยู่แล้ว การข้ามไปเลยช่วยให้รันเร็วขึ้นและ log อ่านง่ายขึ้น

### วิธีเพิ่มหรือเอาเครื่องออกจากรายการ

แก้ที่ **ทุกไฟล์** ที่มีตัวแปรนี้ (5 ไฟล์) ให้ตรงกัน

```python
EXCLUDE_MACHINES = {"AD05", "AD13"}   # ตัดเพิ่ม
EXCLUDE_MACHINES = set()              # ไม่ตัดเครื่องไหนเลย
```

ชื่อเครื่องต้องสะกดตรงกับคอลัมน์ `Machine` ในตาราง `ad_ip` เป๊ะ ๆ (ตัวพิมพ์ใหญ่-เล็กมีผล)

> ⚠️ **ผลข้างเคียงที่ต้องรู้:** เมื่อข้าม AD05 ใน `AD Copy Dat filesv4.py`
> โฟลเดอร์ปลายทาง `\\172.18.106.224\mc_csv6\QC\logad\AD05` จะ**ไม่ถูกล้างอีกต่อไป**
> ไฟล์เก่าที่ค้างอยู่จะคงอยู่ตลอด ถ้าไม่ต้องการต้องเข้าไปลบเอง

### ถ้าอยากแก้ที่ต้นเหตุแทนการตัดออก

ตรวจว่าค่า `IP` และ `Version` ของ AD05 ในตาราง `ad_ip` ยังตรงกับเครื่องจริงหรือไม่

```sql
SELECT Machine, IP, Version FROM vipdatabase.ad_ip WHERE Machine = 'AD05';
```

กลุ่มเครื่องที่มีปัญหาคล้ายกัน (ยังไม่ได้ตัดออก): **AD13** (19 ครั้ง), **AD12** (18), **AD07** (15), **AD09** (14)

---

## 4. สิ่งที่แก้ไปเมื่อ 2026-08-08

| สคริปต์ | อาการเดิม | แก้เป็น |
|---|---|---|
| `AD auto.py` | **ล่มทุกรอบ** — ส่ง list เข้า `os.path.isdir()` เกิด TypeError ที่เครื่องแรกที่ต่อได้ เครื่องที่เหลือไม่ถูกประมวลผลเลย | เช็คด้วย `len()` แทน + ครอบ `try/except` รายเครื่อง |
| `AD auto.py` | อ่านชื่อไฟล์จาก `file.split('\')[5]` ซึ่งได้ค่าผิดเมื่อ path เป็นแบบ `\\IP\Machine\Version\Auto` | ใช้ `os.path.basename()` |
| `AD Log DataV4.py` | **คอลัมน์ `alarm` / `Status` ว่างเปล่าตลอด** — ประกาศ `CAS_KEYS` ไว้แต่ลืมเขียนเงื่อนไขมาใช้ | เพิ่มเงื่อนไข `elif name in CAS_KEYS` |
| ทั้ง `V3_new` และ `V4` | อ่าน section ของ CAS ไปจนจบไฟล์ ทำให้ `Status` ยาวเกินคอลัมน์ (วัดได้ 2238 ตัวอักษร) → MySQL error 1406 → **ทิ้งข้อมูลทีละ 100 แถว** | หยุดอ่านที่หัวข้อ section ถัดไป + จำกัดความยาวที่ `CAS_FIELD_MAX_LEN` |
| ทั้ง `V3_new` และ `V4` | batch ล้มเหลว = ทิ้งทั้ง 100 แถว | ถ้า batch ล้ม จะเปลี่ยนเป็น insert ทีละแถว เสียเฉพาะแถวที่มีปัญหาจริง |
| `AD Log DataV3_new.py` | งานที่ข้ามเที่ยงคืนได้ `finish_datetime` **น้อยกว่า** `start_datetime` | ถ้าเวลาจบน้อยกว่าเวลาเริ่ม ให้บวกไปอีก 1 วัน |
| `recipe.py` | เครื่องที่ไม่มีในตาราง `ad_ip` ทำให้สคริปต์ล่ม เครื่องที่เหลือทั้งหมดไม่ถูกทำ | เช็คก่อนว่าเจอข้อมูลไหม ถ้าไม่เจอให้ข้าม |
| `machine.py` | **เขียน `machine = '8'` ทุกแถว** และกลืน error ทั้งหมดด้วย `except: pass` | **ปลดระวาง** ย้ายไป `backup/` พร้อม `machine.bat` |

---

## 5. เมื่อเกิดปัญหา

### ตรวจ log ก่อนเสมอ

ไฟล์ `ad_log2_run.log` อยู่ในโฟลเดอร์เดียวกับที่รันสคริปต์

| ข้อความใน log | แปลว่า | ต้องทำอะไร |
|---|---|---|
| `[ADxx] ไม่พบ path ที่เข้าถึงได้ — ข้ามเครื่องนี้` | ต่อเครื่องนั้นไม่ได้ | ตรวจ `IP`/`Version` ในตาราง `ad_ip` และตรวจว่าเครื่องเปิดอยู่ |
| `[ADxx] อยู่ใน EXCLUDE_MACHINES — ข้ามเครื่องนี้` | ตั้งใจข้าม ไม่ใช่ error | ถ้าไม่ต้องการให้ข้าม ดูหัวข้อ 3 |
| `ตัด Status จาก NNNN เหลือ 255 ตัวอักษร` | ข้อมูลยาวเกินคอลัมน์ ถูกตัดให้พอดี | ปกติ แต่ถ้าเจอบ่อยควรขยายคอลัมน์ในตาราง |
| `ข้ามแถว id=... machine=...` | แถวนั้นเขียนไม่สำเร็จ (แถวอื่นยังเข้าปกติ) | ดูข้อความ error ท้ายบรรทัดว่าเป็นเพราะอะไร |
| `Insert สำเร็จ 900/1000 แถว` | มี 100 แถวที่หายไป | ค้นหาคำว่า `ข้ามแถว` ใน log เพื่อดูว่าแถวไหน |

### ค่า `CAS_FIELD_MAX_LEN` ยังต้องตรวจสอบ

ตอนนี้ตั้งไว้ที่ **255** แบบคาดเดา ยังไม่ได้ยืนยันกับตารางจริง ให้รันคำสั่งนี้แล้วปรับให้ตรง

```sql
SHOW CREATE TABLE inadatabase.ad_log3;
```

ดูความกว้างจริงของคอลัมน์ `Status` และ `alarm` แล้วแก้ค่า `CAS_FIELD_MAX_LEN` ในไฟล์
`AD Log DataV4.py` และ `AD Log DataV3_new.py` ให้ตรงกัน

### ตรวจว่า `machine.py` เคยทำข้อมูลเสียไว้หรือไม่

```sql
SELECT COUNT(*) FROM inadatabase.ad_log3 WHERE machine = '8';
```

ถ้าเจอแถวแบบนี้ คือร่องรอยของ `machine.py` ที่เคยถูกรัน ข้อมูลพวกนี้ไม่รู้ว่ามาจากเครื่องไหน
เพราะชื่อเครื่องจริงถูกทับไปแล้ว — ต้องตัดสินใจว่าจะลบทิ้งหรือปล่อยไว้

---

## 6. ข้อควรระวังที่ยังไม่ได้แก้

รายการนี้ยัง**ไม่ได้แก้** ในรอบนี้ ตั้งใจแยกไว้ทำต่างหาก

1. **รหัสผ่านฝังในโค้ดแบบข้อความธรรมดา**
   `vip:123456` และ `pe:123456` อยู่ในไฟล์ `.py` ส่วน `imobile:ps@imobile` อยู่ใน `ADCopyDatfiles.bat`
   ใครเปิดไฟล์ก็เห็นหมด ควรย้ายไปเก็บที่อื่นเมื่อมีโอกาส

2. **แถวข้อมูลซ้ำสะสมในตาราง `ad_log3`**
   ทุกรอบที่รันจะดึงข้อมูลย้อนหลังทับช่วงวันเดิม (60 วันหรือ 180 วัน) และตารางไม่มี unique key
   ทำให้แถวเดิมถูกเขียนซ้ำเรื่อย ๆ การแก้ต้องตัดสินใจเรื่องโครงสร้างตารางก่อน
   (เช่น ทำ unique index ที่ `id` + `machine` แล้วใช้ `INSERT ... ON DUPLICATE KEY UPDATE`)

3. **ไม่พบ Scheduled Task ที่ผูกกับสคริปต์กลุ่มนี้**
   ต้องรันด้วยมือ หรือมี task ตั้งไว้ที่เครื่องอื่นที่ยังหาไม่เจอ

---

## 7. โครงสร้างไฟล์

```
D:\BIINA\                          ← โฟลเดอร์ที่ใช้งานจริง
├── AD Log DataV4.py               ← ใช้งาน (อัตโนมัติ 60 วัน)
├── AD Log DataV3_new.py           ← ใช้งาน (รันมือ 180 วัน)
├── AD Copy Dat filesv4.py         ← ใช้งาน
├── AD auto.py                     ← ใช้งาน
├── recipe.py                      ← ใช้งาน
├── *.bat                          ← ตัวเรียกทั้ง 5 ตัว
├── ad_log2_run.log                ← log ที่ระบบเขียน
└── backup\                        ← เวอร์ชันเก่า ห้าม copy ไปใช้
    ├── machine.py, machine.bat    ← ปลดระวาง เขียน machine='8' ผิดทุกแถว
    ├── AD Copy Dat files v1–v3
    ├── AD Log Data v1–V3
    └── README.md                  ← อธิบายว่าแต่ละไฟล์ถูกแทนที่ด้วยอะไร
```

**ห้ามคัดลอกไฟล์ในโฟลเดอร์ `backup\` กลับมาใช้** โดยเฉพาะ `machine.py`
